from interpreter import interpreter

interpreter.llm.api_key = "<...>"
interpreter.auto_run = True
interpreter.llm.model = "gpt-3.5-turbo-0125"

# interpreter.chat("Activate Safari Window") # Executes a single command
